name: 🚀 Deploy AI Vector Search (Streamlit + FastAPI)

on:
  push:
    branches:
      - main
  workflow_dispatch:  # Cho phép trigger thủ công

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: 🚀 Deploy to VPS via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          script_stop: true
          timeout: 30m
          command_timeout: 30m
          script: |
            set -e
            
            echo "=============================================="
            echo "🚀 DEPLOYING AI VECTOR SEARCH DEMO"
            echo "=============================================="
            
            # ============================================
            # CONFIGURATION
            # ============================================
            PROJECT_DIR="/srv/poc_elastik"
            REPO_BRANCH="main"
            
            # Load bash profile để có đủ PATH (nvm, npm, pm2...)
            # Dùng || true để tránh lỗi khi .bashrc có interactive checks
            source "$HOME/.bashrc" 2>/dev/null || true
            source "$HOME/.profile" 2>/dev/null || true
            source "$HOME/.nvm/nvm.sh" 2>/dev/null || true
            
            # Load path cơ bản + npm global
            export PATH=$PATH:/usr/local/bin:/usr/bin:$HOME/.npm-global/bin:$HOME/.local/bin
            
            # ============================================
            # 1. CLONE/UPDATE CODE
            # ============================================
            echo ""
            echo ">>> [1/5] Cập nhật source code..."
            
            # Tạo thư mục nếu chưa có
            if [ ! -d "$PROJECT_DIR" ]; then
              sudo mkdir -p "$PROJECT_DIR"
              sudo chown $USER:$USER "$PROJECT_DIR"
            fi
            
            cd "$PROJECT_DIR"
            
            # Clone code
            if [ ! -d ".git" ]; then
              git clone git@github-poc:${{ github.repository }}.git .
            fi
            
            # Pull code mới
            git fetch origin $REPO_BRANCH
            git reset --hard origin/$REPO_BRANCH
            echo "✅ Code updated"
            
            # ============================================
            # 2. SETUP PYTHON
            # ============================================
            echo ""
            echo ">>> [2/5] Cài đặt thư viện Python..."
            
            PYTHON_CMD="python3"
            if ! command -v python3 &> /dev/null; then PYTHON_CMD="python"; fi
            
            if [ ! -d "venv" ]; then
              $PYTHON_CMD -m venv venv
            fi
            
            source venv/bin/activate
            pip install --upgrade pip -q
            pip install -r requirements.txt -q
            echo "✅ Dependencies installed"
            
            # ============================================
            # 3. LOAD ENV & CHECK ELASTICSEARCH
            # ============================================
            echo ""
            echo ">>> [3/5] Load môi trường & Kiểm tra ES..."
            
            # --- PHẦN QUAN TRỌNG: Load .env an toàn ---
            if [ -f ".env" ]; then
              echo "Loading .env file..."
              set -a  # Tự động export các biến được load
              source .env
              set +a
              echo "✅ Loaded environment variables"
            else
              echo "⚠️  WARNING: .env file not found. App might crash!"
            fi
            
            # Kiểm tra Elasticsearch
            ES_HOST="${ES_HOST:-http://localhost:9200}"
            if curl -s "$ES_HOST" > /dev/null 2>&1; then
              echo "✅ Elasticsearch is running at $ES_HOST"
            else
              echo "⚠️  WARNING: Elasticsearch NOT responding at $ES_HOST"
            fi
            
            # ============================================
            # 4. SETUP SYSTEMD SERVICES
            # ============================================
            echo ""
            echo ">>> [4/5] Cấu hình systemd services..."
            
            VENV_PYTHON="$PROJECT_DIR/venv/bin/python"
            
            # Tạo service file cho FastAPI (dùng sed để strip leading spaces)
            cat << 'FASTAPI_EOF' | sed 's/^[[:space:]]*//' | sudo tee /etc/systemd/system/poc-fastapi.service > /dev/null
            [Unit]
            Description=POC FastAPI Backend
            After=network.target

            [Service]
            Type=simple
            WorkingDirectory=/srv/poc_elastik
            Environment=PATH=/srv/poc_elastik/venv/bin:/usr/local/bin:/usr/bin
            EnvironmentFile=/srv/poc_elastik/.env
            ExecStart=/srv/poc_elastik/venv/bin/python -m uvicorn main:app --host 0.0.0.0 --port 8000
            Restart=always
            RestartSec=3

            [Install]
            WantedBy=multi-user.target
            FASTAPI_EOF
            
            # Tạo service file cho Streamlit
            cat << 'STREAMLIT_EOF' | sed 's/^[[:space:]]*//' | sudo tee /etc/systemd/system/poc-streamlit.service > /dev/null
            [Unit]
            Description=POC Streamlit Frontend
            After=network.target

            [Service]
            Type=simple
            WorkingDirectory=/srv/poc_elastik
            Environment=PATH=/srv/poc_elastik/venv/bin:/usr/local/bin:/usr/bin
            EnvironmentFile=/srv/poc_elastik/.env
            ExecStart=/srv/poc_elastik/venv/bin/python -m streamlit run streamlit_app.py --server.port 8501 --server.address 0.0.0.0 --server.headless true --browser.gatherUsageStats false
            Restart=always
            RestartSec=3

            [Install]
            WantedBy=multi-user.target
            STREAMLIT_EOF
            
            echo "✅ Systemd service files created"
            
            # ============================================
            # 5. RESTART SERVICES
            # ============================================
            echo ""
            echo ">>> [5/6] Khởi động lại services..."
            
            # Reload systemd và restart services
            sudo systemctl daemon-reload
            sudo systemctl enable poc-fastapi poc-streamlit
            sudo systemctl restart poc-fastapi
            sudo systemctl restart poc-streamlit
            
            # Đợi services khởi động
            sleep 3
            
            # ============================================
            # 6. FINISH
            # ============================================
            echo ""
            echo "=============================================="
            echo "✅ DEPLOYMENT SUCCESSFUL!"
            echo "=============================================="
            echo ""
            echo "📍 Services Status:"
            sudo systemctl status poc-fastapi --no-pager -l || true
            echo ""
            sudo systemctl status poc-streamlit --no-pager -l || true
            echo ""
            echo "📍 URLs:"
            echo "   - FastAPI: http://YOUR_SERVER_IP:8000/docs"
            echo "   - Web App: http://YOUR_SERVER_IP:8501"
            echo ""
            echo "📝 Useful commands:"
            echo "   sudo systemctl status poc-fastapi"
            echo "   sudo systemctl status poc-streamlit"
            echo "   sudo journalctl -u poc-fastapi -f"
            echo "   sudo journalctl -u poc-streamlit -f"